program
{
	float x;
	float y;
	x = 10.0;
	y = 20.0;
	
	if (x < y) then {
		if (y > x) then {
			x = 10.0;
			y = 10.0;
			
			if (y >= x) then {
				if (x >= y) then {
					put(12345);
				}else {
					put(-1);
				};
			} else {
				put(-1);
			};
		}else {
			put(-1);
		};
	} else {
		put (-1);
	};
	
	x = 10.0;
	y = 20.0;
	
	if (x > y) then {
		put(-1);
	} else {
		if (y < x) then {
			put(-1);
		} else {
			if (x >= y) then {
				put(-1);
			} else {
				if (y <= x) then {
					put(-1);
				} else {
					put(12345);
				};
			};
		};
	};
};