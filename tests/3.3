program {
	for (int x = 0; x < 10; x = x + 1) {
		for (int y = 0; y < 10; y = y + 1) {
			put (x * 10 + y);
		};
	};
	for (int x = 0; x < 50; x = x + 1) {
		put(x);
	};
	for (int y = 50; y < 100; y = y + 1) {
		put(y);
	};
};